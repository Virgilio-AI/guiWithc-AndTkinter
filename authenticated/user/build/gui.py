
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path
import sys
# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage


OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path("./assets")


def descargarCalificacion():
	print("descargar")


info = sys.argv[1].split(',')
name = info[0]
nombreCompleto = info[1]
carrera = info[2]
año = info[3]
ciudad = info[4]

calificaciones = sys.argv[2].split(',')
calLen = len(calificaciones)

table = [["Materia","calificacion"]]



for i in range(0,calLen,2):
	table.append([calificaciones[i],calificaciones[i+1]])






def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)

window = Tk()

window.geometry("400x600")
window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 300,
    width = 400,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)



can2 = Canvas(
    window,
    bg = "#FFFFFF",
    height = 300,
    width = 400,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

can2.place(x = 0, y = 300)



can2.create_text(
    0.0,
    0.0,
    anchor="nw",
	 text="Calificaciones: ",
    fill="#39B8C0",
    font=("Inter", 15 * -1)
)
startpos = 20,20

for i in range(len(table)):
	for j in range(len(table[i])):
		can2.create_text(
				20.0  + j * 140,
				20.0 + i * 20,
				anchor="nw",
				text=table[i][j],
				fill="#39B8C0",
				font=("Inter", 15 * -1)
				)







canvas.create_text(
    28.0,
    20.0,
    anchor="nw",
    text="Hola " + name,
    fill="#39B8C0",
    font=("Inter", 30 * -1)
)

canvas.create_text(
    23.0,
    96.0,
    anchor="nw",
    text="Nombre Completo:",
    fill="#7053E2",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    23.0,
    130.0,
    anchor="nw",
    text="Carrera:",
    fill="#7053E2",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    23.0,
    164.0,
    anchor="nw",
    text="Año nacimiento:",
    fill="#7053E2",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    23.0,
    198.0,
    anchor="nw",
    text="Ciudad de origen:",
    fill="#7053E2",
    font=("Inter", 15 * -1)
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: descargarCalificacion(),
    relief="flat"
)

button_1.place(
    x=61.0,
    y=523.0,
    width=258.0,
    height=69.0
)

canvas.create_text(
    171.0,
    95.0,
    anchor="nw",
    text=nombreCompleto,
    fill="#000000",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    171.0,
    126.0,
    anchor="nw",
    text=carrera,
    fill="#000000",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    171.0,
    160.0,
    anchor="nw",
    text=año,
    fill="#000000",
    font=("Inter", 15 * -1)
)

canvas.create_text(
    171.0,
    194.0,
    anchor="nw",
    text=ciudad,
    fill="#000000",
    font=("Inter", 15 * -1)
)










window.resizable(False, False)
window.mainloop()
